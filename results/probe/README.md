# Probe

Future explicitly confirmed one-agent probes and one-turn runs are stored in unique run directories. Includes result, response logs, transport API audit and an ineligible research manifest. A one-turn run executes eight country agents, one coordinator and one evaluator (10 calls maximum, zero retries).
