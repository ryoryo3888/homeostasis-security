# Rejected / staging

New paid runs start in a unique directory here. Failures retain checkpoints, API audit and failure status. A completed eight-turn run stays here if research validation fails. Only a validated new run directory may move into `results/research`; existing results are never moved. Resume is not exposed by the unified workflow.
